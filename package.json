{
  "name": "my-link-in-bio",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "dashboard"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "dev:backend": "wrangler dev",
    "dev:dashboard": "bun run --cwd dashboard dev",
    "build:dashboard": "bun run --cwd dashboard build",
    "deploy": "bun run build:dashboard && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "chanfana": "^3.3.0",
    "hono": "^4.12.12",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "wrangler": "^4.92.0"
  }
}
